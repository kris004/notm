# Archive

This directory keeps historical project-planning notes that are no longer the
active documentation for `notm`. They are useful as implementation context, but
the current user-facing and maintainer documentation lives in the root README,
SECURITY.md, the man pages, and the non-archive files under `docs/`.
